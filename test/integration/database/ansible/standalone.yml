---
- name: "Initialize"
  hosts: all
  become: true
  tasks:
    - include: db_volume_init.yml
    - name: test create simple internal database
      manageiq_database:
        state: present
        standalone: true
        cli_path: /opt/rh/cfme-gemset/bin/appliance_console_cli
